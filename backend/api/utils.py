import requests
from django.conf import settings
from django.core.files.base import ContentFile
import uuid
import os

# Utility functions moved to services/image_service.py
# Keeping only generic utilities here if any are needed in the future.
